<h1>New Role</h1>

<%= form_for @role, html: {multipart: true} do |f| %>

	<div>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>


	<%= f.submit "Create Role" %>

<% end %>

<%= link_to "Back to the list", roles_path %>